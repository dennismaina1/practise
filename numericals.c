/* header files */
#include <stdio.h>
/**
 * main- function: print 1-9
 * Return: always 0
 */
int main(void)
{
	float i;

	for (i = 600 ; i < 650 ; i++)
	{
		printf("The numeric value is: %e. \n", i);
	}
	return (0);
}
